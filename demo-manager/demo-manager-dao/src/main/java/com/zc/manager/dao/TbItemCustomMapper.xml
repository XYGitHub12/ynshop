<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zc.manager.dao.TbItemCustomMapper" >

  <sql id="Base_Column_List" >
    i.id, i.title, i.sell_point as sellPoint, i.price, i.num, i.barcode, i.image, i.cid,
    CASE i.status
    WHEN 1 THEN '正常'
    WHEN 2 THEN '下架'
    WHEN 3 THEN '删除'
    ELSE '未知' END AS statusName,
    i.created, i.updated,c.name as catName
  </sql>

  <select id="getCount" resultType="_long">
    SELECT
    COUNT(1)
    FROM
    tb_item AS i
  </select>

  <select id="listItemsByPage" parameterType="com.zc.manager.pojo.dto.PageParam" resultType="com.zc.manager.pojo.vo.TbItemCustom">
    SELECT
    <include refid="Base_Column_List" />
    FROM
    tb_item i left JOIN tb_item_cat c
    ON i.cid = c.id
    ORDER BY id
    limit #{offset},#{limit}
  </select>

</mapper>